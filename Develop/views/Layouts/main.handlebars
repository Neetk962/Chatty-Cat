<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrapcdn">
    <title>Chatty Cat</title>
</head>

<body>
    <header>
        {{!-- page header here AKA page title --}}
    </header>
    <nav>
        <a href="/">Home</a>
        {{#if loggedIn}}
        <button id = "logout">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
    </nav>
    {{!-- add sections or divs/articles based on page setup --}}
    <section class = "">
        {{!-- this {{{ body }}} doesn't have to be here, the body can placed wherever we want the content filled --}}
        {{{ body }}}
    </section>
</body>

</html>